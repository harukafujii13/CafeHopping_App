// src/components/reviewComment/EditReviewButton.tsx
import React from 'react';

interface EditReviewButtonProps {
  onEdit: () => void;
}

const EditReviewButton: React.FC<EditReviewButtonProps> = ({ onEdit }) => {
  return (
    <button
      onClick={onEdit}
      className="text-blue-500 hover:text-blue-700">
      Edit Review
    </button>
  );
};

export default EditReviewButton;
